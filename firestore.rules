rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // No auth, no allowlist -- this trip site is intentionally public.
    match /days/{dayId} {
      allow read, write: if true;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
